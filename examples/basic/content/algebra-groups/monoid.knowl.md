+++
id = "algebra-groups/monoid"
title = "Monoid"
kind = "definition"
summary = "A set with an associative binary operation and an identity element."
aliases = ["monoid", "monoids"]
domains = ["algebra", "group theory"]
+++

A **monoid** is a [[shared-foundations/set|set]] with an associative
[[shared-foundations/binary-operation|binary operation]] and an identity element.

Every [[algebra-groups/group|group]] is a monoid, but not every monoid is a
group.

