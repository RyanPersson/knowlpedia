+++
id = "shared-foundations/set"
title = "Set"
kind = "definition"
summary = "A collection of objects regarded as a single mathematical object."
aliases = ["set", "sets"]
domains = ["foundations"]
+++

A **set** is a collection of objects, called its elements, regarded as a single
mathematical object.

In this prototype, a set is treated as a foundational knowl rather than reduced
to a particular formal set theory.

