+++
id = "shared-foundations/field"
title = "Field"
kind = "definition"
summary = "A set with addition, multiplication, subtraction, and division by nonzero elements."
aliases = ["field", "fields"]
domains = ["foundations", "algebra"]
+++

A **field** is a commutative ring in which every nonzero element has a
multiplicative inverse.

Fields provide the scalars for [[linear-algebra/vector-space|vector spaces]].

