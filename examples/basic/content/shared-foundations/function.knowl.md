+++
id = "shared-foundations/function"
title = "Function"
kind = "definition"
summary = "A rule assigning each input exactly one output."
aliases = ["function", "map", "mapping"]
domains = ["foundations"]
+++

A **function** \(f : X \to Y\) assigns to each element \(x \in X\) exactly one
element \(f(x) \in Y\).

Functions are the basic morphisms in the category of sets.

